<MudForm @ref="MudForm" Model="@milestoneView" Validation="@FieldValidator" @bind-IsValid="@IsValid" @bind-Errors="Errors">
    <MudTextField Label="Név" For="(() => milestoneView.Name)" @bind-Value="milestoneView.Name" OnlyValidateIfDirty="true" Immediate="true" />
    <MudDatePicker For="(() => milestoneView.Planned)" Label="Határidő" @bind-Date="milestoneView.Planned" />
</MudForm>