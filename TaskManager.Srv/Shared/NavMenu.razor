<MudNavMenu>
    <MudNavLink Href="mytasks">Saját feladatok</MudNavLink>

    @if (projectTechnicalName != null){
        <MudNavGroup Title="@projectName" Expanded="true">
            <MudNavLink Href="@($"projects/{projectTechnicalName}/tasks")">Feladatok</MudNavLink>
            <MudNavLink Href="@($"projects/{projectTechnicalName}/milestones")">Mérföldkövek</MudNavLink>
            <MudNavLink Href="@($"projects/{projectTechnicalName}/witemplates")">WI Sablonok</MudNavLink>
        </MudNavGroup>
    }
</MudNavMenu>
